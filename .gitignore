# Secrets / local config -- NEVER commit real credentials (commit .env.example).
.env

# Node
node_modules/
dist/
*.tsbuildinfo
npm-debug.log*

# OS / editor
.DS_Store
.idea/
.vscode/

# Tooling caches
.vitest/

# The pull-request posting flow, lifted out of the build (see backup/RESTORE.md).
# Kept on disk so it can be read and restored; never part of the shipped tool.
backup/
