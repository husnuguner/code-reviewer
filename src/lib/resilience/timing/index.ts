/**
 * The clock a policy reads and the wait it takes.
 *
 * One abstraction, one real implementation; a test supplies its own.
 */

export { systemTimer } from "./system.timer";
export { type ITimer } from "./timer.abstraction";
